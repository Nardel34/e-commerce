{% extends "base.html.twig" %} {% block title %} Création de catégorie {% endblock title %} {% block body %}

<h1>Créer une nouvelle catégorie</h1>

{{form_start(formView)}}
<div class="row mt-3">
    {{form_row(formView.name)}}
</div>

<button type="submit" class="btn btn-primary mt-3"><i class="fas fa-save"></i> Enregistrer</button> {{form_end(formView)}} {% endblock body %}