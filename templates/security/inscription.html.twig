{% extends "base.html.twig" %} {% block title %} Inscription {% endblock title %} {% block body %}
<h1>Inscription</h1>
{{form_start(formView)}}

<div class="col mt-3">{{form_row(formView.email)}}</div>
<div class="col mt-3">{{form_row(formView.password)}}</div>
<div class="col mt-3">{{form_row(formView.fullName)}}</div>

<button class="btn btn-success btn-sm mt-3">S'inscrire</button> {{form_end(formView)}} {% endblock body %}